# Convex (set after `npx convex dev` first run)
VITE_CONVEX_URL=

# Build-time only (used by scripts/, never shipped to the browser)
OPENAI_API_KEY=
ELEVENLABS_API_KEY=
FAL_API_KEY=

# Runtime (Vercel serverless route mints ephemeral tokens — never expose this client-side)
GEMINI_API_KEY=
